#include <stdio.h>
int main() {
int i,n;
int fact=1;
printf("Enter a Number:");
scanf("%d",&n);
if (n<0){
printf("factorial is not defined for negative number");
}
else
    for(i=1;i<=n;i++)
{
    fact=fact*i;
}
printf("factorial of %d=%d\n",n,fact);
return 0;
}
